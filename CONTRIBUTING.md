# 贡献指南

感谢你改进嘴唇闭合提醒器。提交改动前请遵循以下流程。

## 开发环境

```powershell
uv sync --group dev
uv run pytest -q
uv run mouth-monitor
```

## 提交要求

- 保持本地处理，不增加图片、视频或完整关键点上传。
- 不把“持续张嘴检测”描述为“口呼吸诊断”。
- 领域逻辑优先放在可独立测试的模块中。
- 修改校准、状态机、配置或 API 时补充对应测试。
- 停止和暂停路径必须释放摄像头资源。
- 代码注释、用户提示和文档使用中文。

## 验证清单

1. `uv run pytest -q` 全部通过。
2. 启动、暂停、停止均可正常工作。
3. 双点校准完成后重启仍可加载。
4. 持续张嘴只在超过设定时间后提醒。
5. 冷却期间不会连续播放提示音。
